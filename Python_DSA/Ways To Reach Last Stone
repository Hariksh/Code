def count_ways(arr):
    n = len(arr)
    dp = [0] * n
    dp[0] = 1  

    for i in range(n):
        steps = arr[i]
        for j in range(1, steps + 1):
            if i + j < n:
                dp[i + j] += dp[i]

    return dp[n - 1]
