def pairwise_product(N, A):
    result = []
    for i in range(0, N, 2):
        result.append(A[i] * A[i+1])
    
    return result

N = int(input()) 
A = list(map(int, input().split()))
C = pairwise_product(N, A)
print(*C)
