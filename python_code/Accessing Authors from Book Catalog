n = int(input())

book_catalog = {}

for _ in range(n):
    book = input().strip()
    author = input().strip()
    book_catalog[book] = author

authors = list(book_catalog.values())

print(", ".join(authors))
